default:
  --format json --out output/API_Report_(<%= ENV['TEST_ENV_NUMBER'] %>).json
parallel:
  HUB_URL=http://192.168.121.33:4444/wd/hub
  --format json --out output/API_Report_(<%= ENV['TEST_ENV_NUMBER'] %>).json
